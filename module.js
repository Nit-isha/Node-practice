//Modules
// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum)

const { nishu, peter } = require("./moduleName");
const sayHi = require("./moduleUtils");
const data = require("./moduleData");
// console.log(data);
require("./function");

// sayHi("Nishu");
// sayHi("Nitisha");
// sayHi(nishu);
//sayHi(names.nishu)
